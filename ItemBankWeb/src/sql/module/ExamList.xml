<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ExamList">

	<select id="getExamListForUser" parameterType="java.lang.Integer"
		resultType="zh.co.item.bank.model.entity.ExamListModel">
		select a.language, a.exam, a.level, a.year, a.count, a.source, ifnull(b.rate,'0%') as rate
		from tb_exam_list a 
		left join vb_exam_rate b
		on a.source=b.source
		where a.delete_flag!=1 and (user_id=#{userId} or user_id is null)
		order by year+0, count+0;
	</select>

</mapper>